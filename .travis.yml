---
dist: trusty
cache: apt
language: ruby
rvm: 2.2.1
install:
- sudo apt-get update
- sudo apt-get install -y apt-transport-https ca-certificates
- sudo apt-key adv --keyserver hkp://p80.pool.sks-keyservers.net:80 --recv-keys 58118E89F3A912897C070ADBF76221572C52609D
- sudo bash -lec "echo 'deb https://apt.dockerproject.org/repo ubuntu-trusty main'
  > /etc/apt/sources.list.d/docker.list"
- sudo apt-get update
- sudo apt-get install -y docker-engine
- git clone https://github.com/flant/thepackager ~/thepackager
- echo "export BUNDLE_GEMFILE=/~/thepackager/Gemfile" >> ~/.bashrc
- chmod +x /etc/profile.d/thepackager.sh
- cd ~/thepackager
- gem install bundler
- bundle install
script:
- bash -lec "echo HAHA NOTHING HERE"
sudo: required
